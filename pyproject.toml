[project]
name = "ecologits-api"
version = "0.0.1"
description = "Api to use ecologits solution"
requires-python = ">=3.9,<4"

[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
fastapi = {extras = ["standard"], version = "^0.115.12"}
